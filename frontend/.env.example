# Copy to .env.local for development, or .env.production for a build.
#
# Vite inlines this at BUILD time, not run time: rebuild after changing
# it. Without it the bundle calls http://localhost:8000, which is what
# a deployed site will do if you forget.

VITE_API_URL=http://localhost:8000/api/v1
